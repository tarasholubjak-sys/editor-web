# skill-data — авто-генероване

Ця папка генерується скриптом `scripts/copy-skill.mjs` під час `npm install`/`npm run build`.

**Source of truth:** `../../skills/selfy-knowledge-editor/`

⚠️ **НЕ редагуй файли тут руками** — зміни загубляться при наступному білді.

Якщо потрібно правити skill — редагуй у `C:\selfy-outline-mcp\skills\selfy-knowledge-editor\` і запусти `npm run build` в editor-web.
